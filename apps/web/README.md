# FamilyVault Web

Future React/Next.js web app for accessing FamilyVault from a browser.

The web app is not part of the first implementation pass. It will likely be hosted separately, such as on Cloudflare Pages, and will use the same backend permissions model as the Android app.
