# FamilyVault API

Future backend API service, likely built with Node and Hono and deployed to Cloud Run.

This service can hold business logic that should not live in clients, including permission-sensitive workflows, invite flows, document lifecycle operations, and coordination with Supabase.
